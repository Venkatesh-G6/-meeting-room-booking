server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/roombooking_db
    username: root
    password: Venky@112
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.yourcompany.roombooking: DEBUG
